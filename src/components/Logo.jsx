import logo from "../images/logo.png";

const Logo = () => {
  return (
    <label className="logo">
      <img src={logo} alt=""></img>
      <span>Chatnet</span>
    </label>
  );
};

export default Logo;
